<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Tema de ventana (la UI real la pinta Compose). Fondo oscuro de yapi. -->
    <style name="Theme.Yapi" parent="android:Theme.Material.NoActionBar">
        <item name="android:windowBackground">@color/yapi_bg</item>
        <item name="android:statusBarColor">@color/yapi_bg</item>
        <item name="android:navigationBarColor">@color/yapi_bg</item>
        <item name="android:windowLightStatusBar">false</item>
    </style>
</resources>
